pull_request_rules: []
commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
merge_queue:
  mode: serial
  max_parallel_checks: 1
queue_rules:
  - name: vm6-fastforward-review
    batch_size: 1
    queue_conditions: []
    merge_conditions:
      - check-success = vm6-ff-hold-never-20260916
    max_checks_retries: 0
    merge_method: fast-forward
